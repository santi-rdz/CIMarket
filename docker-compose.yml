services:
  backend:
    build: ./backend
    container_name: cimarket-backend
    ports:
      - '8000:8000'
    restart: always
    working_dir: /app/backend
    environment:
      CI: 'true'
      DATABASE_URL: 'mysql://cimarket_user:cimarket_password@mysql:3306/cimarket_db?charset=utf8mb4'
      DATABASE_HOST: mysql
      DATABASE_PORT: '3306'
      DATABASE_USER: cimarket_user
      DATABASE_PASSWORD: cimarket_password
      DATABASE_NAME: cimarket_db
      NODE_ENV: development
      JWT_SECRET: 'dev-only-change-in-production-k8s7d9f2m4p6q1w3e5r7t9y0u2i4o6a8'
      GOOGLE_CLIENT_ID: '140981650188-13cgvaf5oqc5v82rkim1bg3rben2p3qc.apps.googleusercontent.com'
      VAPID_PUBLIC_KEY: 'BDHhh2HDF_fCs1bZhRNglposs40nHuW5WkoFviMRF-AyNYxqY7_lwfRPpwe0XLV1TU8CQVW6vG3Ym-qnZuD2h7w'
      VAPID_PRIVATE_KEY: '9EMIcQOL_N_44HSNSeoNlVvmjO0GnrdP-oLYBx9g1tY'
      VAPID_SUBJECT: 'mailto:dev@cimarket.mx'
    volumes:
      - ./:/app
      - backend_node_modules:/app/node_modules
    command: sh -c "pnpm install && pnpm run prisma:generate && pnpm run db:push && pnpm run db:seed && pnpm run dev"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - cimarket-network

  mysql:
    image: mysql:8.0
    container_name: cimarket-mysql
    restart: always
    command: ['--character-set-server=utf8mb4', '--collation-server=utf8mb4_unicode_ci']
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: cimarket_db
      MYSQL_USER: cimarket_user
      MYSQL_PASSWORD: cimarket_password
    ports:
      - '3306:3306'
    volumes:
      - cimarket_db_data:/var/lib/mysql
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost', '-u', 'cimarket_user', '-pcimarket_password']
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 15s
    networks:
      - cimarket-network

volumes:
  cimarket_db_data:
  backend_node_modules:

networks:
  cimarket-network:
    driver: bridge
